import { GetColTitlePipe } from './get-col-title.pipe';

describe('GetColTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new GetColTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
